<Window x:Class="TweetRex.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:routing="clr-namespace:ReactiveUI.Routing;assembly=ReactiveUI.Routing" 
        Title="TweetRex" Height="350" Width="525">
    <Grid UseLayoutRounding="True">
        <!--
            COOLSTUFF: RoutedViewHost
        
            RoutedViewHost will take a Router and display whatever ViewModel 
            is at the front of the route stack.
        -->
        <routing:RoutedViewHost x:Name="ViewHost" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
    </Grid>

</Window>
